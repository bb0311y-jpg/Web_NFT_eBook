
## 2026-02-04 介面優化 (UI Improvement)
- **遇到的問題**：使用者反饋找不到會員登入入口，首頁只有連接錢包按鈕。
- **嘗試歷程**：
    - 檢查 `page.tsx` Header 區域。
    - 發現只放置了 `ConnectButton`，缺少導向 `/profile` 的入口。
- **分析與修復**：
    - 在 Header 新增 "MEMBER LOGIN" 按鈕。
    - 點擊後直接跳轉至 Member Center，讓無錢包使用者能直觀找到測試入口。
- **成果**：首頁右上角現在有清晰的會員登入選項。
